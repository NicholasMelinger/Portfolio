{% extends "PortfolioBundle:Default:layout2.html.twig" %}

{% block tete %}
<h1>Matrices</h1>
        <p>Sur cette page, vous pouvez gérer les matrices de compétences</p>
{% endblock %}

{% block titre_section %}
	<h1>Gestion d'une matrice</h1>
{% endblock %}

{% block page %}
{% if (app.session.get('userRole') == '1') %}
<div class="form">
 <p>
 	{{ form_start(form_mat) }}
 		{{ form_errors(form_mat) }}
 		<table>
 		<td>
 			<tr>{{ form_row(form_mat.theme_matrice) }}</tr>
 			<tr>{{ form_row(form_mat.s_theme_matrice) }}</tr>
 			<tr>{{ form_row(form_mat.s_s_theme_matrice) }}</tr>
 			<tr>{{ form_row(form_mat.flagActif) }}</tr>
 			<tr><button type="submit" class="btn btn-lg btn-success" >Valider</button></tr>
 		</td>
 	</table>

 	{{ form_end(form_mat) }}
 </p>
</div>
 {% else %}

Page non autorisée.

<a href="{{ path ('portfolio_homepage') }}">Retour à l'accueil</a></li>

{% endif %}
{% endblock %}