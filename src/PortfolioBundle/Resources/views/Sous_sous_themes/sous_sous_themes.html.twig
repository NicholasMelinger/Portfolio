{% extends "PortfolioBundle:Default:layout.html.twig" %}

{% block tete %}
<h1>Sous sous themes</h1>
        <p>Administration des sous sous themes</p>
{% endblock %}

{% block titre_section %}
	<h1>Liste globale des sous sous themes</h1>
{% endblock %}

{% block page %}
{% if (app.session.get('userRole') == '1') %}
<div class="row">
    <div class="col-sm-4">
 		<p>
 			<a class="navbar-brand" href="{{ path ('sous_sous_themes_ajout') }}"><u>Ajouter un sous sous theme</u></a>
		 </p>
	</div>
</div>

{% for sstheme in sous_sous_themes %}
 <p>
<div class="row">
	<div class="col-sm-4">
		<div class="panel panel-primary">
			<div class="panel-heading">
				<h3 class="panel-title"><a href="{{ path ('sous_sous_themes_modifier', { 'id': sstheme.id }) }}"><b><u>{{ sstheme.libelleSousSousTheme }}</u></b></a> - <a href="{{ path ('sous_sous_themes_supprimer', { 'id': sstheme.id }) }}"><u><font color="red">X</font></u></a></h3>
			</div>
		</div>
	</div>
</div>
</p>
{% endfor %}
{% else %}

Page non autorisée.

<a href="{{ path ('portfolio_homepage') }}">Retour à l'accueil</a></li>

{% endif %}
{% endblock %}
